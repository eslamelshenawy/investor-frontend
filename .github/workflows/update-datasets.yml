name: Update Datasets

# يشتغل يومياً الساعة 6 صباحاً (توقيت السعودية = 3 صباحاً UTC)
on:
  schedule:
    - cron: '0 3 * * *'  # كل يوم الساعة 3 صباحاً UTC
  workflow_dispatch:  # للتشغيل اليدوي

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Puppeteer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgbm-dev

      - name: Fetch all datasets metadata
        run: |
          echo "🚀 بدء جلب فهرس البيانات..."
          timeout 2400 node scripts/fetch-all-datasets.cjs || true
          echo "✅ انتهى جلب الفهرس"
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: false

      - name: Fetch top datasets with full data
        run: |
          echo "📊 جلب Top 100 Datasets مع البيانات..."
          timeout 3000 node scripts/fetch-top-datasets.cjs || true
          echo "✅ انتهى جلب البيانات"
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: false

      - name: Check results
        run: |
          echo "📊 فهرس الـ Datasets:"
          cat public/data/datasets.json | head -5
          echo ""
          echo "📁 Top Datasets:"
          ls -la public/data/top-datasets/ 2>/dev/null | head -10 || echo "لا توجد بيانات"
          echo ""
          echo "📈 الإحصائيات:"
          echo "- Datasets في الفهرس: $(cat public/data/datasets.json | grep -c '"id"')"
          echo "- Top Datasets: $(ls public/data/top-datasets/*.json 2>/dev/null | wc -l || echo 0)"

      - name: Commit and push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git add public/data/

          if git diff --staged --quiet; then
            echo "لا توجد تغييرات"
          else
            git commit -m "🔄 Auto-update datasets $(date +'%Y-%m-%d')"
            git push
            echo "✅ تم تحديث البيانات"
          fi
