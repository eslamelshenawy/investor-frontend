name: Update Datasets

# يشتغل يومياً الساعة 6 صباحاً (توقيت السعودية = 3 صباحاً UTC)
on:
  schedule:
    - cron: '0 3 * * *'  # كل يوم الساعة 3 صباحاً UTC
  workflow_dispatch:  # للتشغيل اليدوي

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 300  # 5 ساعات كحد أقصى

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Puppeteer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgbm-dev

      - name: Fetch ALL datasets metadata (11,500+)
        run: |
          echo "🚀 بدء جلب كل الـ 11,500 dataset..."
          echo "⏱️ قد يستغرق 2-4 ساعات..."
          timeout 14400 node scripts/fetch-all-datasets.cjs || true
          echo "✅ انتهى جلب الفهرس"
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: false

      - name: Check results
        run: |
          echo ""
          echo "════════════════════════════════════════"
          echo "📊 النتائج النهائية"
          echo "════════════════════════════════════════"
          TOTAL=$(cat public/data/datasets.json | grep -c '"id"' || echo 0)
          echo "✅ عدد الـ Datasets: $TOTAL"
          echo ""
          head -10 public/data/datasets.json
          echo "..."

      - name: Commit and push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git add public/data/

          if git diff --staged --quiet; then
            echo "لا توجد تغييرات"
          else
            git commit -m "🔄 Auto-update datasets $(date +'%Y-%m-%d')"
            git push
            echo "✅ تم تحديث البيانات"
          fi
